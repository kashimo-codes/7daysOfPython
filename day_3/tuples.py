# Tuples
# Similar to lists but are immutable (cannot be changed)
# used to repr fixed sets of data.
#  Can only be created with or without parenthesis ()

my_tuple = (1,2,'three',[4,5])
print(my_tuple[1]) # prints out 2
print(my_tuple[3][0]) # prints out 4

